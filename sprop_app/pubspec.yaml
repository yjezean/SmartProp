name: compost_monitor_app
description: IoT Compost Monitoring System Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.1
  
  # Networking
  mqtt_client: ^10.0.0
  http: ^1.1.0
  
  # UI
  fl_chart: ^0.65.0
  syncfusion_flutter_gauges: ^24.1.41
  
  # Utilities
  intl: ^0.18.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

